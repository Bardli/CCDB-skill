# Personal-config overrides (account names, venv paths, group memberships)
# Keep these in Claude memory at ~/.claude/projects/<proj>/memory/personal_cc_config.md
# instead of committing them here.
personal-overrides.md
personal_*.md

# Editor / OS junk
.DS_Store
*~
.swp
